name: Danish News Bot - Multiple Daily Runs

on:
  schedule:
    # Расписание по UTC (GitHub Actions работает в UTC)
    # Датское время летом (CEST = UTC+2): -2 часа от датского времени
    # Датское время зимой (CET = UTC+1): -1 час от датского времени
    - cron: '30 5 * * *'   # 05:30 UTC = 07:30 датского времени (летом)
    - cron: '30 7 * * *'   # 07:30 UTC = 09:30 датского времени (летом)
    - cron: '0 10 * * *'   # 10:00 UTC = 12:00 датского времени (летом)
    - cron: '0 11 * * *'   # 11:00 UTC = 12:00 датского времени (летом)
    - cron: '0 12 * * *'   # 12:00 UTC = 14:00 датского времени (летом)
    - cron: '0 14 * * *'   # 14:00 UTC = 16:00 датского времени (летом)
    - cron: '0 17 * * *'   # 17:00 UTC = 19:00 датского времени (летом)
    - cron: '0 18 * * *'   # 18:00 UTC = 20:00 датского времени (летом)
    - cron: '0 19 * * *'   # 19:00 UTC = 21:00 датского времени (летом)
  workflow_dispatch: # Ручной запуск для тестирования

env:
  GO_VERSION: '1.23'

jobs:
  send-single-news:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Go
      uses: actions/setup-go@v5
      with:
        go-version: ${{ env.GO_VERSION }}

    - name: Cache Go modules
      uses: actions/cache@v3
      with:
        path: |
          ~/.cache/go-build
          ~/go/pkg/mod
        key: ${{ runner.os }}-go-${{ hashFiles('**/go.sum') }}
        restore-keys: |
          ${{ runner.os }}-go-

    - name: Download dependencies
      run: |
        echo "📦 Downloading Go modules..."
        go mod download
        go mod tidy
        go mod verify
        echo "✅ Dependencies ready"

    - name: Build application
      run: |
        echo "🔨 Building application..."
        echo "📁 Working directory: $(pwd)"
        echo "📄 Module info: $(go list -m)"
        mkdir -p bin
        go build -v -o bin/dknews .
        echo "✅ Build completed successfully"
        ls -la bin/

    - name: Run news bot (single news mode)
      env:
        TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
        TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        BOT_MODE: single
        DEBUG: false
      run: |
        echo "🚀 Starting Danish News Bot at $(date)"
        echo "📅 Current time UTC: $(date -u)"
        echo "🇩🇰 Danish time: $(TZ='Europe/Copenhagen' date)"
        echo "🎯 Target chat: $TELEGRAM_CHAT_ID"
        echo "🤖 Bot mode: $BOT_MODE"
        if [ -z "$GEMINI_API_KEY" ]; then echo '❌ GEMINI_API_KEY missing'; exit 1; fi
        if [ -z "$TELEGRAM_TOKEN" ]; then echo '❌ TELEGRAM_TOKEN missing'; exit 1; fi
        if [ -z "$TELEGRAM_CHAT_ID" ]; then echo '❌ TELEGRAM_CHAT_ID missing'; exit 1; fi
        echo "🏃 Running bot..."
        ./bin/dknews

    - name: Check for errors
      if: failure()
      run: |
        echo "❌ Job failed at $(date)"
        echo "📋 Environment check:"
        echo "- Go version: $(go version)"
        echo "- Working directory: $(pwd)"
        echo "- Files: $(ls -la)"
        echo "- Go mod status: $(head -5 go.mod)"
        echo "- Module list: $(go list -m all | head -10)"
        echo "- Built binary exists: $(ls -la bin/ || echo 'No bin directory')"
